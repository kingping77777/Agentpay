# ── Core backend (database layer + API) ──────────────────────────────────────
# Install this first: pip install -r requirements.txt
fastapi==0.115.0
uvicorn[standard]==0.30.6

# SQLAlchemy async + psycopg3 (pre-built wheels for Python 3.13 on Windows)
sqlalchemy[asyncio]==2.0.35
psycopg[binary]==3.2.3

# Migrations
alembic==1.13.2

# Settings, validation, auth
pydantic-settings==2.5.2
pydantic[email]==2.9.2
python-dotenv==1.0.1
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0

# Payments
razorpay==1.4.1

# HTTP + misc
httpx==0.27.2
python-multipart==0.0.12

# NOTE: langchain + openai are in requirements-agents.txt
# Install those AFTER the database layer is working.

